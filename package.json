{
  "name": "simple-logger",
  "version": "0.0.0-development",
  "description": "> Provide the basic logging mechanism.  It can be easy to inject the storage logic for those logging result.",
  "main": "logging.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha tests",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "runtest": "mocha tests --recursive --watch",
    "semantic-release": "semantic-release pre && npm public && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leocwlam/simple-logger.git"
  },
  "keywords": [],
  "author": "Leo Lam",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/leocwlam/simple-logger/issues"
  },
  "homepage": "https://github.com/leocwlam/simple-logger#readme",
  "dependencies": {
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.2",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "semantic-release": "^15.8.1"
  }
}
